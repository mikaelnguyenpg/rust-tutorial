info:
  name: update user
  type: http
  seq: 2

http:
  method: PUT
  url: http://localhost:3000/api/users/2
  body:
    type: json
    data: |-
      {
        "id":2,
        "name": "The first user",
        "email": "user@email.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
