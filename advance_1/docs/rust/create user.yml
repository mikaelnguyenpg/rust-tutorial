info:
  name: create user
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/user
  body:
    type: json
    data: |-
      {
        "id":22,
        "name": "The first user",
        "email": "useremail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
